/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pngimg.com', 'www.google.com','mpng.subpng.com'],
  },

}

module.exports = nextConfig
